# Task Breakdown: Initial Implementation

> **Fase SDD: Tasks** — Traduce `Plan` ([02-plan.md](02-plan.md)) en tareas concretas y accionables, listas para pasar a `Implement` (el código, que todavía no existe en este repo) y verificarse en `Validate` ([04-validate.md](04-validate.md)). Es un desglose propuesto, no un compromiso de alcance: se debe revisar y refinar antes de empezar a programar.

## Frontend (Next.js / React)
- [x] Scaffold de la app Next.js — `frontend/` (TypeScript, App Router, Tailwind).
- [x] Pantalla de perfil/preferencias con sliders de ponderación (costo / variedad / tiempo de preparación) que sumen 100% — `frontend/src/app/preferences/page.tsx`. Los sliders redistribuyen proporcionalmente para garantizar suma exacta de 100 (`frontend/src/lib/preferences.ts`, con tests).
- [x] Integración con Supabase (auth + queries) desde el frontend — `frontend/src/lib/supabase/client.ts`; auth por magic link, lee/escribe la tabla `preferences`.
- [x] Deploy a Vercel (`food-opt.vercel.app`, root directory `frontend/`, env vars apuntando al proyecto cloud). El import automático de Vercel detectó el monorepo como "Services" e intentó deployar `backend/` (Python) como un segundo servicio — hubo que forzar el Framework Preset a "Next.js" manualmente y redeployar, si no quedaba serviendo 404 en todas las rutas pese a compilar bien.
- [x] Probar la interacción real en navegador (sliders, magic link, guardado) contra producción (food-opt.vercel.app) — flujo completo confirmado: login por magic link, mover sliders, "Guardar" escribe en la tabla `preferences` real. En el camino aparecieron y se corrigieron 3 bugs reales (ver Data layer): Vercel deployeando como "Other" en vez de Next.js, Site URL de Auth apuntando a localhost, y falta de trigger `profiles`. Ver issue [#1](https://github.com/leonidasf300/food_opt/issues/1).
- [x] Pantalla de objetivos nutricionales — `frontend/src/app/targets/page.tsx`. Lee/escribe `user_nutrient_targets` (min/max de calorías, proteína, grasa, carbohidratos).
- [x] Pantalla de generación de plan — `frontend/src/app/plan/page.tsx`. Junta preferencias + objetivos guardados y le pide un plan al backend (`NEXT_PUBLIC_BACKEND_API_URL`, local por ahora). Muestra el plan día a día más una tabla "Lista de compras" (cantidad necesaria, unidades a comprar redondeadas, costo) devuelta por el backend. Probado end-to-end en el navegador real: login → guardar preferencias → guardar objetivos → generar plan → plan real devuelto por el solver, incluida la lista de compras verificada a mano.
- [x] Login local (magic link vía Mailpit) rompía en silencio: la home page (`/`) no importa el cliente de Supabase, así que el fragmento `#access_token=...` del callback nunca se procesaba si el usuario aterrizaba ahí — la página se quedaba en blanco para siempre sin ningún error en consola. Fix: `signInWithOtp` ahora pasa `emailRedirectTo` explícito a `/preferences` (la página que sí instancia el cliente). Encontrado probando el flujo real en el navegador, no por inspección de código.

## Data layer (Supabase)
- [x] Esquema: usuarios, preferencias, recetas, ingredientes, valores nutricionales — `supabase/migrations/20260921140828_create_core_schema.sql`. Verificado corriendo local (Docker), ver `supabase/README.md`.
- [x] Políticas de acceso (RLS) para datos de usuario — mismo archivo de migración.
- [x] Otorgar privilegios explícitos a nivel de tabla (`GRANT`) al rol `authenticated` — `supabase/migrations/20260921161544_grant_table_privileges.sql`. RLS sola no alcanza: Postgres exige `GRANT` antes de evaluar las políticas. Verificado end-to-end contra la API REST real (no como superusuario) con `auto_expose_new_tables = false` (la config recomendada para el proyecto cloud): `anon` da 401, `authenticated` lee `ingredients` pero no la escribe (403), y lee/escribe su propio `profiles`/`preferences`.
- [x] Crear proyecto Supabase cloud real (`food-opt`, ref `fufzybvwbdgejnozrgoa`, región us-east-1) y hacer `supabase db push` — las 5 migraciones aplicadas. Verificado contra la API REST real: `anon` da 401, igual que en local.
- [x] Trigger que crea `public.profiles` al hacer signup — `supabase/migrations/20260921194319_create_profile_on_signup.sql`. Faltaba: `preferences`/`user_nutrient_targets`/`recipes` referencian `profiles`, y sin este trigger cualquier usuario nuevo rompía con "violates foreign key constraint" al primer guardado. Incluye backfill para usuarios ya registrados antes del fix. Ver issue [#1](https://github.com/leonidasf300/food_opt/issues/1).
- [x] Configurar SMTP propio (Resend) para Auth — el servicio de email default de Supabase tiene un rate limit demasiado bajo para uso real (~2-4/hora). En modo test (sin dominio propio verificado en Resend) solo manda a la casilla registrada en la cuenta de Resend; verificar un dominio queda pendiente para producción real.
- [x] Corregir Site URL de Supabase Auth (apuntaba a `http://localhost:3000`, ahora a `https://food-opt.vercel.app`) — los magic links redirigían a local en vez de al sitio real.
- [x] Importar snapshot inicial de USDA FoodData Central a Supabase — 15/15 ingredientes, corrido y verificado contra **local y contra el proyecto cloud real** (`supabase/scripts/import_usda.py`). Los ingredientes se fetchean por `fdcId` fijo, no por búsqueda de texto: la búsqueda por relevancia de USDA dio 2 matches incorrectos en el primer intento (confundió "sweet potato, raw" con "Sweet Potato puffs, frozen", y "oats" con "Oil, oat") — corregido pinneando el `fdcId` verificado de cada ingrediente en `seed_ingredients.py`. Para apuntar a cloud hay que usar el connection pooler, no el host directo (que es IPv6-only) — ver `supabase/scripts/README.md`.
- [ ] Evaluar APIs comerciales (Edamam/Nutritionix) para cubrir productos de marca faltantes en el snapshot de USDA.
- [ ] Definir proceso de curación manual para huecos restantes (ingredientes sin match en ninguna fuente).
- [ ] Definir mercado objetivo (USA / Colombia / ambos) y, según eso, fuente de precios real (candidatas: Kroger Developer API para USA; DANE SIPSA para Colombia). Mientras tanto, precios de prueba estimados a mano en `purchase_unit_price` de los 15 ingredientes (`supabase/scripts/set_placeholder_prices.py`) — sirven para ejercitar el optimizador localmente, **no son precios reales**, están marcados como placeholder en el código.
- [x] Recetas de prueba (8) usando los 15 ingredientes de USDA — `supabase/scripts/seed_recipes.py` + `import_recipes.py`.
- [x] Vista `recipe_totals`: agrega costo y nutrientes por receta en un solo lugar (evita duplicar la lógica en Python y en JS) — `supabase/migrations/20260921201908_create_recipe_totals_view.sql`. Encontramos y corregimos un bug de fan-out SQL en el camino: el JOIN con `ingredient_nutrients` (4 filas por ingrediente, una por nutriente) inflaba el `sum(costo)` 4x porque no dependía de qué nutriente fuera la fila — los nutrientes con `FILTER` no se veían afectados, por eso pasó desapercibido hasta comparar contra el cálculo a mano. Verificado: ahora coincide exacto con el script de import.

## Backend de optimización
- [x] Setup del proyecto Python con Pyomo + HiGHS (ver [00-constitution.md](00-constitution.md)) — `backend/`.
- [x] Implementar función objetivo multi-criterio (costo, variedad, tiempo) — `backend/food_opt/model.py`.
- [x] Implementar restricciones nutricionales diarias (macro/micro). Decisión: sin horizonte semanal, ver [00-constitution.md](00-constitution.md).
- [x] Normalizar los tres objetivos antes de combinarlos — método de tabla de pagos (payoff table / Marler & Arora), `backend/food_opt/model.py`. **Bug real encontrado después probando la API real**: la rama `maximize=True` de `_normalized()` tenía el signo invertido — subir el peso de variedad no aumentaba la variedad (la empujaba para abajo). Ver issue [#2](https://github.com/leonidasf300/food_opt/issues/2), ya cerrado. Test de regresión agregado.
- [x] Exponer el modelo como API HTTP local — `backend/food_opt/api.py` (FastAPI, `POST /plans`), `backend/food_opt/data.py` (trae recetas desde la vista `recipe_totals`). No deployado en ningún lado, solo `uvicorn food_opt.api:app --port 8000` para desarrollo local. Probado con curl y con el flujo real en el navegador (genera un plan de 3 días usando las 8 recetas de prueba).
- [x] Implementar redondeo a unidades de compra comercial — `backend/food_opt/shopping_list.py`, `math.ceil(cantidad_necesaria / purchase_unit_size)`.
- [x] Implementar agregación de lista de compras semanal — mismo módulo, `build_shopping_list()`: suma la cantidad necesaria de cada ingrediente entre todas las recetas/porciones del plan (usa `recipe_ingredients` vía `data.py::fetch_recipe_ingredients()`) antes de redondear a unidades de compra. Expuesto en `POST /plans` (`PlanResponse.shopping_list`) y renderizado en `frontend/src/app/plan/page.tsx` como tabla "Lista de compras". Verificado a mano contra la API real: 480g de frijoles negros → 2 bolsas → $3.60; 600g de arroz blanco → 1 bolsa → $2.50; y de nuevo en el flujo E2E por navegador (160g frijoles negros por 2 porciones → 1 bolsa → $1.80). `shopping_list.py` es un módulo puro sin DB, igual que `model.py`.

## Testing (según 04-validate.md)
- [x] Unit tests del modelo: cumplimiento de restricciones nutricionales, indicador de variedad, sensibilidad al peso de costo, consistencia y acotamiento de la normalización, y que subir el peso de variedad realmente use más recetas distintas (test de regresión del bug del issue #2) — `backend/tests/test_model.py`.
- [x] Unit tests: lógica de redondeo y agregación de lista de compras — `backend/tests/test_shopping_list.py` (agregación entre recetas que comparten ingrediente, redondeo hacia arriba, múltiplo exacto de la unidad de compra no redondea de más, porciones en cero se excluyen, ingrediente sin precio cargado reporta cantidad sin costo, orden alfabético).
- [ ] Integration tests automatizados: Supabase ↔ backend de optimización (hoy probado a mano, no como test automatizado).
- [x] E2E manual: flujo completo preferencias → objetivos → generar plan, probado en el navegador real contra local (Supabase + API + frontend, los tres corriendo). Falta automatizar esto como test.
